export enum GameStatus {
  WaitingForStart,
  WaitingForInitialSentences,
  WaitingForDrawings,
  WaitingForSentences,
  DrawingShowcase,
  Finished,
}

export enum EntryType {
  Drawing,
  Sentence,
}

export enum QuestType {
  Drawing,
  Sentence,
}
